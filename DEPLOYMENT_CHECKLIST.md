# Чеклист передачи проекта на Windows

## ✅ Что нужно сделать перед отправкой в Git

### 1. Проверить .gitignore
- [x] Создан корневой .gitignore
- [x] node_modules исключены
- [x] .env файлы исключены
- [x] Flutter build папки исключены
- [x] IDE файлы исключены

### 2. Проверить, что все файлы на месте
```bash
# Проверьте структуру:
tree -L 2 -I 'node_modules|build|.git'
```

Должно быть:
```
.
├── backend/
│   ├── src/
│   ├── prisma/
│   ├── Dockerfile
│   ├── package.json
│   └── tsconfig.json
├── web/
│   ├── src/
│   ├── Dockerfile
│   ├── package.json
│   └── vite.config.ts
├── booking_flutter/
│   ├── lib/
│   ├── windows/
│   ├── ios/
│   ├── android/
│   └── pubspec.yaml
├── docker-compose.yml
├── README.md
├── WINDOWS_SETUP.md
└── .gitignore
```

### 3. Проверить конфигурационные файлы

#### docker-compose.yml
- [x] Правильные порты (4000, 3000, 5432)
- [x] Правильные environment переменные
- [x] Volumes настроены
- [x] Healthcheck для БД

#### backend/package.json
- [x] Все зависимости указаны
- [x] Scripts настроены (dev, build)
- [x] Prisma seed настроен

#### web/package.json
- [x] Все зависимости указаны
- [x] Scripts настроены (dev, build)

#### booking_flutter/pubspec.yaml
- [x] Все зависимости указаны
- [x] Версии совместимы

### 4. Проверить документацию

- [x] README.md - общее описание
- [x] WINDOWS_SETUP.md - инструкция для Windows
- [x] backend/README.md - backend документация
- [x] web/README.md - web документация
- [x] booking_flutter/README.md - Flutter документация
- [x] BACKEND_CHECKLIST.md - выполненные требования backend
- [x] WEB_CHECKLIST.md - выполненные требования web
- [x] FLUTTER_CHECKLIST.md - выполненные требования Flutter

### 5. Удалить чувствительные данные

Проверьте, что НЕ попали в Git:
- [ ] .env файлы с реальными паролями
- [ ] node_modules
- [ ] build папки
- [ ] IDE конфигурации (.idea, .vscode)
- [ ] Личные данные

### 6. Проверить, что проект работает

```bash
# Остановите все:
docker compose down -v

# Удалите node_modules:
rm -rf backend/node_modules web/node_modules

# Запустите заново:
docker compose up --build

# Проверьте:
# - http://localhost:4000/graphql
# - http://localhost:3000
```

## 📤 Отправка в Git

### 1. Инициализация (если еще не сделано)
```bash
git init
git add .
git commit -m "Initial commit: Mini Booking System"
```

### 2. Добавление remote
```bash
git remote add origin <URL_ВАШЕГО_РЕПОЗИТОРИЯ>
git branch -M main
git push -u origin main
```

### 3. Проверка
```bash
# Посмотрите, что отправлено:
git log --oneline

# Проверьте статус:
git status
```

## 📋 Что отправить коллеге на Windows

### Минимальный набор:
1. **URL Git репозитория**
2. **Файл WINDOWS_SETUP.md** (можно отдельно отправить)
3. **Список требований**:
   - Docker Desktop for Windows
   - Flutter SDK
   - Visual Studio 2022 (для Flutter Windows)

### Инструкция для коллеги:

```
Привет! Вот проект Mini Booking System.

Что нужно установить:
1. Git - https://git-scm.com/download/win
2. Docker Desktop - https://www.docker.com/products/docker-desktop/
3. Flutter SDK - https://docs.flutter.dev/get-started/install/windows
4. Visual Studio 2022 Community - https://visualstudio.microsoft.com/downloads/
   (выбери "Desktop development with C++")

Как запустить:
1. Клонируй репозиторий:
   git clone <URL>
   cd mini_booking

2. Запусти Docker Desktop

3. Запусти backend и web:
   docker compose up --build

4. Открой в браузере:
   - http://localhost:4000/graphql (GraphQL API)
   - http://localhost:3000 (Web приложение)

5. Для Flutter Windows:
   cd booking_flutter
   flutter pub get
   flutter run -d windows

Подробная инструкция в файле WINDOWS_SETUP.md

Если что-то не работает - пиши!
```

## 🔍 Проверка на чистой системе

Идеально протестировать на чистой Windows машине или виртуалке:

1. Клонировать репозиторий
2. Следовать WINDOWS_SETUP.md
3. Проверить, что все работает
4. Записать видео-демо

## 📝 Дополнительные файлы (опционально)

### .editorconfig
Для единообразного форматирования:
```ini
root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
indent_style = space
indent_size = 2

[*.md]
trim_trailing_whitespace = false
```

### .dockerignore
Для backend и web (уже созданы):
```
node_modules
dist
.env
*.log
.DS_Store
```

## ⚠️ Важные замечания

### Для Windows пользователя:

1. **Docker Desktop должен быть запущен** перед выполнением docker команд
2. **PowerShell или Command Prompt** - используйте любой
3. **Пути в Windows** используют обратный слеш `\`, но в командах можно использовать `/`
4. **Антивирус** может блокировать Docker - добавьте в исключения
5. **WSL2** должен быть включен для Docker Desktop

### Различия Mac vs Windows:

| Аспект | Mac | Windows |
|--------|-----|---------|
| Терминал | Terminal/iTerm | PowerShell/CMD |
| Пути | `/Users/...` | `C:\Users\...` |
| Docker | Native | WSL2 |
| Flutter | Native | Native |
| Слеш | `/` | `\` (но `/` тоже работает) |

## 🎯 Финальная проверка

Перед отправкой убедитесь:

- [ ] Проект собирается на вашей машине
- [ ] Docker compose работает
- [ ] Web приложение открывается
- [ ] Flutter компилируется
- [ ] Все README файлы актуальны
- [ ] .gitignore настроен правильно
- [ ] Нет чувствительных данных в Git
- [ ] WINDOWS_SETUP.md содержит все необходимое

## 📞 Поддержка

Если у коллеги возникнут проблемы:

1. Попросите прислать:
   - Вывод `docker compose logs`
   - Вывод `flutter doctor -v`
   - Скриншот ошибки

2. Проверьте:
   - Docker Desktop запущен?
   - Порты свободны?
   - Flutter установлен правильно?

3. Типичные проблемы:
   - Порт занят → `netstat -ano | findstr :4000`
   - Docker не работает → Перезапустить Docker Desktop
   - Flutter ошибки → `flutter doctor` покажет что нужно

## ✅ Готово к передаче!

После выполнения всех пунктов проект готов к передаче на Windows.
