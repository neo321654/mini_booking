# Быстрый старт на macOS

## ✅ Что уже установлено у вас:
- ✅ Docker Desktop
- ✅ Flutter SDK
- ✅ Xcode (для iOS/macOS)

## 🚀 Запуск проекта

### 1. Backend и Web (через Docker)

```bash
# Убедитесь что Docker Desktop запущен

# Запустите backend и web:
docker compose up --build

# Или в фоновом режиме:
docker compose up --build -d
```

**Проверьте:**
- GraphQL API: http://localhost:4000/graphql
- Web приложение: http://localhost:3000

### 2. Flutter macOS Desktop Widget

```bash
# В новом терминале:
cd booking_flutter

# Установите зависимости (если еще не сделали):
flutter pub get

# Запустите macOS приложение:
flutter run -d macos

# Или с hot reload:
flutter run -d macos --hot
```

**Что вы увидите:**
- Компактное desktop приложение
- 2 отеля со статистикой
- Статус номеров (свободно/занято)
- Ближайшая бронь
- Кнопка обновления

### 3. Flutter iOS (опционально)

```bash
# Откройте iOS симулятор:
open -a Simulator

# Запустите приложение:
flutter run

# Выберите iOS устройство из списка
```

**Что вы увидите:**
- Полное мобильное приложение
- Список отелей и номеров
- Создание и отмена броней
- Pull-to-refresh

## 🎯 Различия между платформами

### macOS Desktop Widget (то же что Windows)
- Компактный интерфейс мониторинга
- Только просмотр статистики
- Автообновление каждые 15 секунд
- Кнопка ручного обновления

### iOS/Android Mobile
- Полнофункциональное приложение
- Навигация между экранами
- Создание и отмена броней
- Выбор дат через календарь

## 🔧 Полезные команды

```bash
# Посмотреть доступные устройства:
flutter devices

# Остановить Docker:
docker compose down

# Посмотреть логи backend:
docker compose logs backend

# Очистить Flutter кеш:
flutter clean
flutter pub get

# Проверить код:
flutter analyze
```

## 🐛 Troubleshooting

### Backend не запускается:
```bash
docker compose logs backend
docker compose down -v
docker compose up --build
```

### Flutter не находит macOS:
```bash
flutter doctor -v
# Проверьте что Xcode установлен
```

### Порт занят:
```bash
# Найти процесс:
lsof -ti:4000

# Убить процесс:
lsof -ti:4000 | xargs kill -9
```

## ✅ Готово!

Теперь у вас работает:
- ✅ Backend GraphQL API
- ✅ Web приложение
- ✅ macOS desktop виджет
- ✅ (опционально) iOS мобильное приложение

Все компоненты подключены к одному backend и работают с одними данными!
