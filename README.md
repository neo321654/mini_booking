# Mini Booking System

Система бронирования отелей с GraphQL API (Node.js + Apollo), мобильным приложением (Flutter), десктоп виджетом (Flutter Windows) и веб-интерфейсом (React + TypeScript).

## 🚀 Быстрый старт

### Требования
- Docker и Docker Compose
- Node.js 20+ (для локальной разработки)
- Flutter SDK (для мобильного и десктоп приложений)

### Запуск через Docker

```bash
# Клонируйте репозиторий
git clone <repository-url>
cd mini-booking

# Запустите все сервисы
docker compose up --build
```

После запуска:
- **GraphQL API**: http://localhost:4000
- **GraphQL Playground**: http://localhost:4000/graphql
- **PostgreSQL**: localhost:5432

### Локальная разработка Backend

```bash
cd backend

# Установите зависимости
npm install

# Запустите PostgreSQL через Docker
docker compose up db -d

# Примените миграции
npx prisma migrate dev

# Заполните БД тестовыми данными
npx prisma db seed

# Запустите сервер
npm run dev
```

## 📊 Структура данных

### Модели
- **Hotel** - отели (2 отеля в seed данных)
- **Room** - номера (по 3 номера в каждом отеле)
- **Booking** - брони (6 тестовых броней для демонстрации)

### Тестовые данные
После seed в БД будут:
- 2 отеля: "Grand Plaza Hotel" и "Ocean View Resort"
- 6 номеров (по 3 в каждом отеле)
- 6 броней с различными сценариями (прошлые, текущие, будущие, последовательные)

## 🔧 GraphQL API

### Queries

```graphql
# Получить все отели
query {
  hotels {
    id
    name
    rooms {
      id
      name
    }
  }
}

# Получить номера отеля
query {
  rooms(hotelId: "hotel-id") {
    id
    name
    bookings {
      id
      startDate
      endDate
    }
  }
}

# Получить брони номера
query {
  bookings(roomId: "room-id") {
    id
    startDate
    endDate
  }
}
```

### Mutations

```graphql
# Проверить доступность
mutation {
  checkAvailability(
    roomId: "room-id"
    startDate: "2026-03-10"
    endDate: "2026-03-15"
  )
}

# Создать бронь
mutation {
  createBooking(
    roomId: "room-id"
    startDate: "2026-03-10"
    endDate: "2026-03-15"
  ) {
    id
    startDate
    endDate
  }
}

# Отменить бронь
mutation {
  cancelBooking(bookingId: "booking-id")
}
```

## 🛡️ Валидация и бизнес-логика

- ✅ Даты начала должны быть раньше дат окончания
- ✅ Нельзя бронировать даты в прошлом
- ✅ Брони не должны пересекаться (проверка в транзакции)
- ✅ Логирование всех ключевых операций (создание/отмена броней)
- ✅ Обработка ошибок с понятными сообщениями

## 📝 Логирование

Backend использует Pino для структурированного логирования:
- Создание броней
- Отмена броней
- Конфликты при бронировании
- Ошибки валидации
- GraphQL ошибки

## 🗄️ База данных

### Подключение к PostgreSQL

```bash
# Через Docker
docker exec -it booking_db psql -U postgres -d booking

# Локально
psql -h localhost -U postgres -d booking
```

### Prisma Studio

```bash
cd backend
npx prisma studio
```

Откроется веб-интерфейс на http://localhost:5555

## 🧪 Тестирование API

### Примеры запросов

1. **Получить все отели с номерами**
```bash
curl -X POST http://localhost:4000/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"{ hotels { id name rooms { id name } } }"}'
```

2. **Создать бронь**
```bash
curl -X POST http://localhost:4000/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"mutation { createBooking(roomId: \"room-id\", startDate: \"2026-04-01\", endDate: \"2026-04-05\") { id startDate endDate } }"}'
```

## 🏗️ Архитектура

```
mini-booking/
├── backend/              # Node.js + Apollo GraphQL
│   ├── src/
│   │   └── index.ts     # GraphQL schema и resolvers
│   ├── prisma/
│   │   ├── schema.prisma # Модели данных
│   │   └── seed.ts      # Тестовые данные
│   ├── Dockerfile
│   └── package.json
├── docker-compose.yml    # Оркестрация сервисов
└── README.md
```

## 🔍 Troubleshooting

### Backend не запускается
```bash
# Проверьте логи
docker compose logs backend

# Пересоздайте контейнеры
docker compose down -v
docker compose up --build
```

### База данных не доступна
```bash
# Проверьте статус PostgreSQL
docker compose ps

# Проверьте логи БД
docker compose logs db
```

### Ошибки миграций
```bash
# Сбросьте БД и примените миграции заново
docker compose down -v
docker compose up --build
```

## 📹 Видео-демо

[Ссылка на видео будет добавлена после записи]

## 🎯 Выполненные требования

### Backend ✅
- [x] Node.js + Apollo GraphQL + TypeScript
- [x] PostgreSQL через Prisma ORM
- [x] Seed данных (2 отеля, 6 номеров, 6 броней)
- [x] Валидация дат и бизнес-логика
- [x] Логирование (Pino) ключевых операций
- [x] Обработка ошибок
- [x] Проверка пересечений броней в транзакции

### Docker ✅
- [x] docker-compose.yml для backend + PostgreSQL
- [x] Healthcheck для БД
- [x] Автоматические миграции и seed при старте
- [x] Volume для персистентности данных

### Документация ✅
- [x] README с инструкциями
- [x] Примеры GraphQL запросов
- [x] Troubleshooting секция

## 📄 Лицензия

MIT
