# Инструкция по запуску на Windows

Это руководство поможет запустить проект Mini Booking на Windows.

## 📋 Требования

### Обязательно:
1. **Git** - для клонирования репозитория
2. **Docker Desktop for Windows** - для backend и web
3. **Flutter SDK** - для мобильного и desktop приложения

### Опционально:
- **Node.js 20+** - если хотите запускать backend локально без Docker
- **Visual Studio Code** - рекомендуемый редактор

## 🚀 Шаг 1: Установка необходимого ПО

### 1.1 Git
```powershell
# Скачайте и установите Git с официального сайта:
# https://git-scm.com/download/win

# Проверьте установку:
git --version
```

### 1.2 Docker Desktop
```powershell
# Скачайте и установите Docker Desktop:
# https://www.docker.com/products/docker-desktop/

# После установки запустите Docker Desktop
# Убедитесь, что Docker работает:
docker --version
docker compose version
```

**Важно**: Docker Desktop должен быть запущен перед выполнением команд docker.

### 1.3 Flutter SDK
```powershell
# Скачайте Flutter SDK:
# https://docs.flutter.dev/get-started/install/windows

# Распакуйте в C:\src\flutter (или другую папку)

# Добавьте в PATH:
# Системные переменные → Path → Добавить → C:\src\flutter\bin

# Проверьте установку:
flutter --version
flutter doctor
```

**Примечание**: `flutter doctor` покажет, что нужно установить дополнительно.

### 1.4 Visual Studio (для Flutter Windows)
```powershell
# Для Flutter Windows desktop нужен Visual Studio 2022
# Скачайте Community Edition:
# https://visualstudio.microsoft.com/downloads/

# При установке выберите:
# - Desktop development with C++
```

## 📥 Шаг 2: Клонирование репозитория

```powershell
# Откройте PowerShell или Command Prompt

# Перейдите в нужную папку:
cd C:\Projects

# Клонируйте репозиторий:
git clone <URL_ВАШЕГО_РЕПОЗИТОРИЯ>

# Перейдите в папку проекта:
cd mini_booking
```

## 🐳 Шаг 3: Запуск Backend и Web через Docker

### 3.1 Запуск всех сервисов

```powershell
# Убедитесь, что Docker Desktop запущен!

# Запустите все сервисы:
docker compose up --build

# Или в фоновом режиме:
docker compose up --build -d
```

**Что запустится:**
- PostgreSQL база данных (порт 5432)
- Backend GraphQL API (порт 4000)
- Web приложение (порт 3000)

### 3.2 Проверка работы

Откройте в браузере:
- **GraphQL Playground**: http://localhost:4000/graphql
- **Web приложение**: http://localhost:3000

### 3.3 Просмотр логов

```powershell
# Все логи:
docker compose logs

# Логи backend:
docker compose logs backend

# Логи web:
docker compose logs web

# Следить за логами в реальном времени:
docker compose logs -f
```

### 3.4 Остановка сервисов

```powershell
# Остановить:
docker compose down

# Остановить и удалить данные:
docker compose down -v
```

## 📱 Шаг 4: Запуск Flutter приложения

### 4.1 Установка зависимостей

```powershell
# Перейдите в папку Flutter проекта:
cd booking_flutter

# Установите зависимости:
flutter pub get

# Проверьте, что нет ошибок:
flutter analyze
```

### 4.2 Запуск Windows Desktop приложения

```powershell
# Убедитесь, что backend запущен (docker compose up)!

# Проверьте доступные устройства:
flutter devices

# Должно показать что-то вроде:
# Windows (desktop) • windows • windows-x64 • Microsoft Windows

# Запустите приложение:
flutter run -d windows

# Или с hot reload:
flutter run -d windows --hot
```

**Что вы увидите:**
- Компактный виджет мониторинга
- 2 отеля со статистикой
- Статус номеров (свободно/занято)
- Кнопка обновления

### 4.3 Запуск Android эмулятора (опционально)

```powershell
# Установите Android Studio:
# https://developer.android.com/studio

# Создайте виртуальное устройство через AVD Manager

# Запустите эмулятор:
flutter emulators
flutter emulators --launch <emulator_id>

# Запустите приложение:
flutter run
```

## 🔧 Шаг 5: Локальная разработка (без Docker)

Если хотите запускать backend локально:

### 5.1 Установка Node.js

```powershell
# Скачайте Node.js 20 LTS:
# https://nodejs.org/

# Проверьте:
node --version
npm --version
```

### 5.2 Запуск PostgreSQL

```powershell
# Запустите только базу данных:
docker compose up db -d
```

### 5.3 Запуск Backend

```powershell
# Перейдите в папку backend:
cd backend

# Установите зависимости:
npm install

# Примените миграции:
npx prisma migrate dev

# Заполните тестовыми данными:
npx prisma db seed

# Запустите сервер:
npm run dev
```

### 5.4 Запуск Web

```powershell
# Откройте новый терминал
# Перейдите в папку web:
cd web

# Установите зависимости:
npm install

# Запустите dev сервер:
npm run dev
```

## 🐛 Troubleshooting (Решение проблем)

### Проблема: Docker не запускается

**Решение:**
1. Убедитесь, что Docker Desktop запущен
2. Проверьте, что виртуализация включена в BIOS
3. Перезапустите Docker Desktop

### Проблема: Порт уже занят

```powershell
# Найдите процесс на порту 4000:
netstat -ano | findstr :4000

# Убейте процесс (замените PID):
taskkill /PID <PID> /F
```

### Проблема: Flutter не находит устройства

```powershell
# Проверьте установку:
flutter doctor -v

# Установите недостающие компоненты
```

### Проблема: Backend не подключается к БД

**Решение:**
1. Проверьте, что PostgreSQL запущен: `docker compose ps`
2. Проверьте логи: `docker compose logs db`
3. Пересоздайте контейнеры: `docker compose down -v && docker compose up --build`

### Проблема: Flutter Windows не компилируется

**Решение:**
1. Установите Visual Studio 2022 с C++ tools
2. Запустите: `flutter doctor`
3. Следуйте инструкциям flutter doctor

### Проблема: Web приложение не подключается к backend

**Решение:**
1. Проверьте, что backend запущен на порту 4000
2. Откройте http://localhost:4000/graphql в браузере
3. Проверьте консоль браузера (F12) на ошибки CORS

## 📁 Структура проекта

```
mini_booking/
├── backend/              # Node.js + Apollo GraphQL
│   ├── src/
│   ├── prisma/
│   ├── Dockerfile
│   └── package.json
├── web/                  # React + TypeScript
│   ├── src/
│   ├── Dockerfile
│   └── package.json
├── booking_flutter/      # Flutter (Mobile + Windows)
│   ├── lib/
│   ├── windows/
│   └── pubspec.yaml
├── docker-compose.yml    # Оркестрация сервисов
└── README.md
```

## 🎯 Проверка работоспособности

### Чеклист:
- [ ] Docker Desktop запущен
- [ ] `docker compose up` работает без ошибок
- [ ] http://localhost:4000/graphql открывается
- [ ] http://localhost:3000 открывается
- [ ] Flutter приложение компилируется
- [ ] Flutter приложение подключается к backend

### Тестовые сценарии:

1. **Backend API**:
   - Откройте http://localhost:4000/graphql
   - Выполните запрос:
   ```graphql
   query {
     hotels {
       id
       name
       rooms {
         id
         name
       }
     }
   }
   ```

2. **Web приложение**:
   - Откройте http://localhost:3000
   - Должны увидеть список отелей
   - Кликните на номер
   - Попробуйте создать бронь

3. **Flutter Windows**:
   - Запустите `flutter run -d windows`
   - Должен открыться виджет с отелями
   - Нажмите кнопку Refresh
   - Данные должны обновиться

## 📞 Получение помощи

Если что-то не работает:

1. Проверьте логи:
   ```powershell
   docker compose logs
   ```

2. Проверьте статус контейнеров:
   ```powershell
   docker compose ps
   ```

3. Проверьте Flutter:
   ```powershell
   flutter doctor -v
   ```

4. Посмотрите документацию:
   - README.md - общая информация
   - backend/README.md - backend документация
   - web/README.md - web документация
   - booking_flutter/README.md - Flutter документация

## 🎬 Запись видео-демо

После успешного запуска можно записать видео:

1. Запустите все сервисы
2. Откройте OBS Studio или другую программу записи
3. Покажите:
   - GraphQL Playground с запросами
   - Web приложение (создание/отмена броней)
   - Flutter Windows виджет
   - Конфликты броней

## 📝 Дополнительные команды

### Docker:
```powershell
# Пересобрать без кеша:
docker compose build --no-cache

# Посмотреть использование ресурсов:
docker stats

# Очистить все:
docker system prune -a
```

### Flutter:
```powershell
# Очистить кеш:
flutter clean

# Обновить зависимости:
flutter pub upgrade

# Собрать release версию:
flutter build windows --release
```

### Node.js:
```powershell
# Очистить node_modules:
rmdir /s /q node_modules
npm install

# Обновить зависимости:
npm update
```

## ✅ Готово!

Теперь проект должен работать на Windows. Если возникнут проблемы, проверьте раздел Troubleshooting или создайте issue в репозитории.
