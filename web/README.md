# Web Application - Mini Booking System

React + TypeScript + Apollo Client веб-приложение для системы бронирования отелей.

## 🎯 Функциональность

### Реализовано:
- ✅ Просмотр списка отелей и номеров
- ✅ Просмотр броней для каждого номера
- ✅ Проверка доступности номера на выбранные даты
- ✅ Создание новой брони
- ✅ Отмена существующей брони
- ✅ Обновление данных (кнопка Refresh)

## 🏗️ Архитектура

### Структура проекта:
```
web/
├── src/
│   ├── components/          # React компоненты
│   │   ├── HotelList.tsx   # Список отелей и номеров
│   │   └── RoomDetails.tsx # Детали номера и управление бронями
│   ├── graphql/            # GraphQL запросы и мутации
│   │   ├── queries.ts      # Queries (получение данных)
│   │   └── mutations.ts    # Mutations (изменение данных)
│   ├── types/              # TypeScript типы
│   │   └── index.ts        # Интерфейсы для Hotel, Room, Booking
│   ├── App.tsx             # Главный компонент
│   ├── App.css             # Стили
│   └── main.tsx            # Точка входа + Apollo Client setup
├── index.html              # HTML шаблон
├── vite.config.ts          # Конфигурация Vite
└── package.json            # Зависимости
```

## 🔧 Технологии

- **React 18** - UI библиотека
- **TypeScript** - типизация
- **Apollo Client** - GraphQL клиент
- **Vite** - сборщик и dev сервер
- **CSS** - стилизация (без фреймворков)

## 🚀 Запуск

### Через Docker (рекомендуется):
```bash
# Из корня проекта
docker compose up web --build
```

### Локально:
```bash
cd web

# Установить зависимости
npm install

# Запустить dev сервер (backend должен быть запущен на :4000)
npm run dev
```

Приложение будет доступно на http://localhost:3000

## 📖 Как использовать

### 1. Просмотр отелей и номеров
- Левая панель показывает все отели и их номера
- Для каждого номера видно количество броней
- Кликните на номер, чтобы увидеть детали

### 2. Создание брони
1. Выберите номер из списка
2. В правой панели выберите даты начала и окончания
3. Нажмите "Check Availability" для проверки доступности
4. Если номер доступен, нажмите "Create Booking"

### 3. Просмотр существующих броней
- В разделе "Existing Bookings" видны все брони номера
- Показаны даты и ID брони

### 4. Отмена брони
- Нажмите кнопку "Cancel" рядом с бронью
- Подтвердите действие в диалоге

### 5. Обновление данных
- Нажмите кнопку "🔄 Refresh" в шапке для обновления всех данных

## 🎨 UI/UX особенности

- **Адаптивный дизайн** - работает на разных размерах экрана
- **Визуальная обратная связь** - индикаторы загрузки, сообщения об успехе/ошибке
- **Валидация форм** - проверка дат на клиенте
- **Цветовая схема** - фиолетовый градиент для акцентов
- **Интуитивная навигация** - двухпанельный интерфейс

## 🔍 Компоненты

### App.tsx
Главный компонент приложения:
- Управляет состоянием выбранного номера
- Выполняет запрос GET_HOTELS для получения данных
- Координирует работу дочерних компонентов

### HotelList.tsx
Отображает список отелей и номеров:
- Группирует номера по отелям
- Показывает количество броней для каждого номера
- Обрабатывает выбор номера

### RoomDetails.tsx
Управляет бронями номера:
- Форма создания новой брони
- Проверка доступности
- Список существующих броней
- Отмена броней

## 🔌 Apollo Client

### Конфигурация:
```typescript
const client = new ApolloClient({
  uri: 'http://localhost:4000/graphql',
  cache: new InMemoryCache(),
});
```

### Используемые хуки:
- `useQuery` - для получения данных (GET_HOTELS)
- `useMutation` - для изменения данных (CREATE_BOOKING, CANCEL_BOOKING, CHECK_AVAILABILITY)

## 🐛 Обработка ошибок

Приложение обрабатывает следующие ошибки:
- Ошибки сети (backend недоступен)
- Ошибки валидации (неверные даты)
- Конфликты броней (номер уже забронирован)
- Ошибки GraphQL (от backend)

Все ошибки отображаются пользователю в понятном виде.

## 📝 TypeScript типы

```typescript
interface Hotel {
  id: string;
  name: string;
  rooms: Room[];
}

interface Room {
  id: string;
  name: string;
  hotelId: string;
  bookings: Booking[];
}

interface Booking {
  id: string;
  roomId: string;
  startDate: string;
  endDate: string;
}
```

## 🔄 Обновление данных

После создания или отмены брони:
1. Показывается сообщение об успехе
2. Через 1.5 секунды автоматически обновляются данные
3. Пользователь видит актуальное состояние

## 🎯 Соответствие требованиям задания

### Обязательные требования:
- ✅ React + TypeScript
- ✅ Apollo Client для GraphQL
- ✅ Список отелей/номеров
- ✅ Просмотр броней номера
- ✅ Создание брони
- ✅ Отмена брони
- ✅ Docker контейнер

### Дополнительно реализовано:
- ✅ Проверка доступности перед бронированием
- ✅ Валидация дат на клиенте
- ✅ Красивый UI с градиентами
- ✅ Адаптивный дизайн
- ✅ Обработка всех ошибок
- ✅ Индикаторы загрузки
- ✅ Автообновление после операций

## 🚀 Production Build

```bash
# Собрать для production
npm run build

# Результат в папке dist/
# Можно деплоить на любой статический хостинг
```

## 📦 Зависимости

### Runtime:
- `react` - UI библиотека
- `react-dom` - рендеринг в DOM
- `@apollo/client` - GraphQL клиент
- `graphql` - GraphQL core

### Development:
- `typescript` - типизация
- `vite` - сборщик
- `@vitejs/plugin-react` - React плагин для Vite
- `@types/react` - типы для React
- `@types/react-dom` - типы для ReactDOM
